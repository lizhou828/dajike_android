<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.djk_shop"
          android:versionCode="1"
          android:versionName="1.0">
    <uses-sdk android:minSdkVersion="8" android:targetSdkVersion="16"/>
    
    <uses-permission android:name="android.permission.READ_PHONE_STATE"></uses-permission>

    <application android:name=".MyApplication" android:label="@string/app_name" android:icon="@drawable/ic_launcher">

        <activity android:name="MyActivity"
                  android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".LoginActivity" android:theme="@style/login_head"></activity>
        <activity android:name=".RegisterActivity" android:theme="@style/login_head"></activity>
        <activity android:name=".PortalActivity" android:theme="@style/login_head"></activity>
        <activity android:name=".AActivity" android:theme="@style/login_head"></activity>
        <activity android:name=".BActivity" android:theme="@style/login_head"></activity>
        <activity android:name=".CActivity" android:theme="@style/login_head"></activity>
        <activity android:name=".UserCenterActivity" android:theme="@style/login_head"></activity>
        <activity android:name=".EActivity" android:theme="@style/login_head"></activity>
        <activity android:name=".MobileAppInfoActivity" android:theme="@style/login_head"></activity>


    </application>
</manifest>
